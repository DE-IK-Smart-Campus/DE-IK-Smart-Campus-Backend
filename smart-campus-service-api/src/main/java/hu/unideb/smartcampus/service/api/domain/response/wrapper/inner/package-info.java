/**
 * These wrapper classes are members of another wrapper classes, so they do not have to set the
 * BaseWrapper's class messageType.
 */
package hu.unideb.smartcampus.service.api.domain.response.wrapper.inner;
